# Project Instructions

This is a claude code plugin which installs multiple sub agents and commands.

## Exclusions

**NEVER** read, search, or glob in these directories:
- `node_modules/`
- `.git/`
- `dist/`
- `build/`
- `*.lock` files (package-lock.json, yarn.lock, etc.)
